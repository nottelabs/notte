---
title: "PageFetchResponse"
---
import AgentMdNotice from '/partials/agent-md-notice.mdx';

<AgentMdNotice />

{/* Generated by node-sdk/scripts/generate-reference.mjs. Do not edit. */}

[Source: node-sdk/src/page-fetch.ts](https://github.com/nottelabs/notte/blob/main/node-sdk/src/page-fetch.ts#L234)

The response of `session.fetch()`, shaped like the Web `Response` with the
final `url` after redirects exposed as a plain property. A non-2xx status is
a response, not an error; `raiseForStatus()` throws `PageFetchHTTPError`.
`bytes()` holds the exact bytes the server sent, so binary bodies survive;
`text()` decodes them with the declared charset, or utf-8 when none is given.

```typescript
import { PageFetchResponse } from 'notte-sdk';
```

```typescript
new PageFetchResponse(init: { status: number; headers: Record<string, string>; url: string; content: Uint8Array; }): PageFetchResponse;
```

<ParamField body="init" type={"{ status: number; headers: Record<string, string>; url: string; content: Uint8Array }"} required>

</ParamField>



## Usage

- [bytes](/typescript-sdk-reference/pagefetchresponse/bytes): The raw body bytes.
- [arrayBuffer](/typescript-sdk-reference/pagefetchresponse/arraybuffer)
- [text](/typescript-sdk-reference/pagefetchresponse/text)
- [json](/typescript-sdk-reference/pagefetchresponse/json)
- [raiseForStatus](/typescript-sdk-reference/pagefetchresponse/raiseforstatus): Throw `PageFetchHTTPError` on a 4xx/5xx status, like `requests.Response.raise_for_status()`.

<Accordion title="Properties">

### status

```typescript
status: number
```



### statusText

```typescript
statusText: string
```



### headers

```typescript
headers: Headers
```



### url

```typescript
url: string
```

The final URL after redirects.

### encoding

```typescript
encoding: string | undefined
```

The declared charset, `utf-8` when the body is valid utf-8, else `undefined`.

### ok

```typescript
ok: boolean
```



</Accordion>
