---
title: "ReplayOptions"
---
import AgentMdNotice from '/partials/agent-md-notice.mdx';

<AgentMdNotice />

{/* Generated by node-sdk/scripts/generate-reference.mjs. Do not edit. */}

[Source: node-sdk/src/session.ts](https://github.com/nottelabs/notte/blob/main/node-sdk/src/session.ts#L66)

```typescript
export interface ReplayOptions {
  /** Poll until the replay is ready instead of failing on 404. Defaults to true. */
  wait?: boolean;
  /** Maximum time to wait for the replay, in milliseconds. Defaults to 240 000. */
  timeoutMs?: number;
  /** Delay between polling attempts, in milliseconds. Defaults to 5 000. */
  pollIntervalMs?: number;
}
```

## Fields

<ParamField body="wait" type={"boolean | undefined"}>
Poll until the replay is ready instead of failing on 404. Defaults to true.
</ParamField>

<ParamField body="timeoutMs" type={"number | undefined"}>
Maximum time to wait for the replay, in milliseconds. Defaults to 240 000.
</ParamField>

<ParamField body="pollIntervalMs" type={"number | undefined"}>
Delay between polling attempts, in milliseconds. Defaults to 5 000.
</ParamField>
