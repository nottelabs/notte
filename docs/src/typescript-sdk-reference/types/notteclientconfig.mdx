---
title: "NotteClientConfig"
---
import AgentMdNotice from '/partials/agent-md-notice.mdx';

<AgentMdNotice />

{/* Generated by node-sdk/scripts/generate-reference.mjs. Do not edit. */}

[Source: node-sdk/src/client.ts](https://github.com/nottelabs/notte/blob/main/node-sdk/src/client.ts#L68)

```typescript
export interface NotteClientConfig {
  /** API base URL. Defaults to `NOTTE_API_URL` or `https://api.notte.cc`. */
  baseUrl?: string;
  /** API key. Defaults to `NOTTE_API_KEY`. Optional only for relative proxy base URLs. */
  apiKey?: string;
  /** Default per-request timeout in milliseconds. Defaults to 60 000, like the Python SDK. */
  timeoutMs?: number;
  /** Database preview branch. Defaults to `NOTTE_DB_PREVIEW_BRANCH`. Internal. */
  dbPreview?: string;
  /** Log every request like `NotteClient(verbose=True)` in Python. */
  verbose?: boolean;
}
```

## Fields

<ParamField body="baseUrl" type={"string | undefined"}>
API base URL. Defaults to `NOTTE_API_URL` or `https://api.notte.cc`.
</ParamField>

<ParamField body="apiKey" type={"string | undefined"}>
API key. Defaults to `NOTTE_API_KEY`. Optional only for relative proxy base URLs.
</ParamField>

<ParamField body="timeoutMs" type={"number | undefined"}>
Default per-request timeout in milliseconds. Defaults to 60 000, like the Python SDK.
</ParamField>

<ParamField body="dbPreview" type={"string | undefined"}>
Database preview branch. Defaults to `NOTTE_DB_PREVIEW_BRANCH`. Internal.
</ParamField>

<ParamField body="verbose" type={"boolean | undefined"}>
Log every request like `NotteClient(verbose=True)` in Python.
</ParamField>
