---
title: "FunctionRunOptions"
---
import AgentMdNotice from '/partials/agent-md-notice.mdx';

<AgentMdNotice />

{/* Generated by node-sdk/scripts/generate-reference.mjs. Do not edit. */}

[Source: node-sdk/src/functions.ts](https://github.com/nottelabs/notte/blob/main/node-sdk/src/functions.ts#L22)

Select extended for longer cloud execution. Defaults to standard (Lambda).

```typescript
export type FunctionRunOptions = Pick<RunFunctionRequest, 'runtime' | 'stream'> & {
	/** Stream logs and wait for completion. Defaults to true. */
	stream?: boolean;
	/** Receive streamed logs instead of printing them to console.log. */
	onLog?: (message: string) => void;
	/** Throw when execution fails. Defaults to true, matching Python. */
	raiseOnFailure?: boolean;
	/** Execute a record returned by createRun(). Omit to create a new run. */
	functionRunId?: GetFunctionRunResponse['function_run_id'];
};
```

## Fields

<ParamField body="stream" type={"boolean | undefined"}>
Whether to stream logs, or only return final response
Stream logs and wait for completion. Defaults to true.
</ParamField>

<ParamField body="runtime" type={"\"standard\" | \"extended\" | null | undefined"}>
Override the saved function runtime; omit to inherit its default
</ParamField>

<ParamField body="onLog" type={"((message: string) => void) | undefined"}>
Receive streamed logs instead of printing them to console.log.
</ParamField>

<ParamField body="raiseOnFailure" type={"boolean | undefined"}>
Throw when execution fails. Defaults to true, matching Python.
</ParamField>

<ParamField body="functionRunId" type={"string | undefined"}>
Execute a record returned by createRun(). Omit to create a new run.
</ParamField>

## Related types

- [GetFunctionRunResponse](/typescript-sdk-reference/types/getfunctionrunresponse)
- [RunFunctionRequest](/typescript-sdk-reference/types/runfunctionrequest)
