---
title: "SessionScriptOptions"
---
import AgentMdNotice from '/partials/agent-md-notice.mdx';

<AgentMdNotice />

{/* Generated by node-sdk/scripts/generate-reference.mjs. Do not edit. */}

[Source: node-sdk/src/session.ts](https://github.com/nottelabs/notte/blob/main/node-sdk/src/session.ts#L75)

```typescript
export interface SessionScriptOptions {
  /** Return a standalone workflow script rather than only the relevant steps. Defaults to true. */
  as_workflow?: boolean;
  /** Infer a `response_format` schema for scrape calls that have instructions but no schema. */
  infer_response_format?: boolean;
}
```

## Fields

<ParamField body="as_workflow" type={"boolean | undefined"}>
Return a standalone workflow script rather than only the relevant steps. Defaults to true.
</ParamField>

<ParamField body="infer_response_format" type={"boolean | undefined"}>
Infer a `response_format` schema for scrape calls that have instructions but no schema.
</ParamField>
