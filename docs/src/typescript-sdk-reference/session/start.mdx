---
title: "start"
---
import AgentMdNotice from '/partials/agent-md-notice.mdx';

<AgentMdNotice />

{/* Generated by node-sdk/scripts/generate-reference.mjs. Do not edit. */}

[Source: node-sdk/src/session.ts](https://github.com/nottelabs/notte/blob/main/node-sdk/src/session.ts#L183)

Start the session: creates a new session on the server.

Retries up to `SESSION_START_TRIES` times on 5xx errors, waiting
`CLUSTER_OVERLOAD_RETRY_DELAY_MS` on HTTP 529 (cluster overload). 4xx
errors are never retried.

Prefer `session.use(async session => { ... })`, which also stops the session.

```typescript
Session.start(): Promise<void>;
```

## Returns

```typescript
Promise<void>
```

## Example

```typescript
const session = client.Session();
await session.start();
```
