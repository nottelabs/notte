---
title: "evaluateJs"
---
import AgentMdNotice from '/partials/agent-md-notice.mdx';

<AgentMdNotice />

{/* Generated by node-sdk/scripts/generate-reference.mjs. Do not edit. */}

[Source: node-sdk/src/session.ts](https://github.com/nottelabs/notte/blob/main/node-sdk/src/session.ts#L725)

Evaluate JavaScript on the current page and return its result as a string.

The result is stringified the way `evaluate_js` records it: objects and
arrays as JSON, a JS `null` as the string `"null"`. On failure the typed
`ActionExecutionError` is thrown with the actual JavaScript error; pass
`raiseOnFailure: false` to get the execution result envelope instead.

```typescript
Session.evaluateJs(code: string, options?: { raiseOnFailure?: true; }): Promise<string>;
Session.evaluateJs(code: string, options: { raiseOnFailure: false; }): Promise<ApiExecutionResponse>;
```

## Parameters (overload 1)

<ParamField body="code" type={"string"} required>

</ParamField>

<ParamField body="options" type={"{ raiseOnFailure?: true }"}>

</ParamField>

## Returns (overload 1)

```typescript
Promise<string>
```

## Parameters (overload 2)

<ParamField body="code" type={"string"} required>

</ParamField>

<ParamField body="options" type={"{ raiseOnFailure: false }"} required>

</ParamField>

## Returns (overload 2)

```typescript
Promise<ApiExecutionResponse>
```

## Example

```typescript
const title = await session.evaluateJs('document.title');
const payload = JSON.parse(await session.evaluateJs('(async () => JSON.stringify(await (await fetch("/api")).json()))()'));
```

## Related types

- [ApiExecutionResponse](/typescript-sdk-reference/types/apiexecutionresponse)
