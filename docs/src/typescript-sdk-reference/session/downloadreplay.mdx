---
title: "downloadReplay"
---
import AgentMdNotice from '/partials/agent-md-notice.mdx';

<AgentMdNotice />

{/* Generated by node-sdk/scripts/generate-reference.mjs. Do not edit. */}

[Source: node-sdk/src/session.ts](https://github.com/nottelabs/notte/blob/main/node-sdk/src/session.ts#L459)

Download the MP4 replay to a local file, like `ReplayResponse.download()`
in Python. Resolves with the absolute path of the written file and throws
when the replay has no `mp4_url`.

```typescript
Session.downloadReplay(path?: string, options?: ReplayOptions): Promise<string>;
```

## Parameters

<ParamField body="path" type={"string"}>
Default:

```typescript
'replay.mp4'
```
</ParamField>

<ParamField body="options" type={"ReplayOptions"}>
Default:

```typescript
{}
```
</ParamField>

## Returns

```typescript
Promise<string>
```

## Example

```typescript
await session.stop();
const file = await session.downloadReplay('session.mp4');
```

## Related types

- [ReplayOptions](/typescript-sdk-reference/types/replayoptions)
