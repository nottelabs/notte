---
title: "replay"
---
import AgentMdNotice from '/partials/agent-md-notice.mdx';

<AgentMdNotice />

{/* Generated by node-sdk/scripts/generate-reference.mjs. Do not edit. */}

[Source: node-sdk/src/session.ts](https://github.com/nottelabs/notte/blob/main/node-sdk/src/session.ts#L412)

Get presigned URLs for the session replay. Recordings are finalized after
the session is closed, so by default this polls on 404 until the replay is
ready. It keeps working after `stop()`.

```ts
await session.stop();
const replay = await session.replay();
console.log(replay.mp4_url);
```

Throws an `Error` when the API reports the session is still active and a
`NotteTimeoutError` when the replay is not ready within `timeoutMs`.

```typescript
Session.replay(options?: ReplayOptions): Promise<ReplayResponse>;
```

## Parameters

<ParamField body="options" type={"ReplayOptions"}>
Default:

```typescript
{}
```
</ParamField>

## Returns

```typescript
Promise<ReplayResponse>
```

## Related types

- [ReplayOptions](/typescript-sdk-reference/types/replayoptions)
- [ReplayResponse](/typescript-sdk-reference/types/replayresponse)
