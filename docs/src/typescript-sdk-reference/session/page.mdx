---
title: "page"
---
import AgentMdNotice from '/partials/agent-md-notice.mdx';

<AgentMdNotice />

{/* Generated by node-sdk/scripts/generate-reference.mjs. Do not edit. */}

[Source: node-sdk/src/session.ts](https://github.com/nottelabs/notte/blob/main/node-sdk/src/session.ts#L597)

Get a Playwright page connected to the session via CDP. The connection is
established lazily on first access, cached, and closed by `stop()`.
Native dialogs are left to the Notte backend.

Requires the optional `playwright-core` peer dependency
(`npm install playwright-core`).

```ts
const page = await session.page();
await page.goto('https://www.google.com');
await page.screenshot({ path: 'screenshot.png' });
```

```typescript
Session.page(): Promise<Page>;
```

## Returns

```typescript
Promise<Page>
```
