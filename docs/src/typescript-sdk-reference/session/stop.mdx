---
title: "stop"
---
import AgentMdNotice from '/partials/agent-md-notice.mdx';

<AgentMdNotice />

{/* Generated by node-sdk/scripts/generate-reference.mjs. Do not edit. */}

[Source: node-sdk/src/session.ts](https://github.com/nottelabs/notte/blob/main/node-sdk/src/session.ts#L270)

Stop the session and clean up resources (including any Playwright
connection opened with `page()`). When a `cookie_file` was given, the
session cookies are appended to it first. A session the API reports as
already stopped is tolerated with a warning.

```ts
await session.stop();
```

```typescript
Session.stop(closeReason?: SessionCloseReason): Promise<void>;
```

## Parameters

<ParamField body="closeReason" type={"SessionCloseReason"}>
Default:

```typescript
'manual'
```
</ParamField>

## Returns

```typescript
Promise<void>
```

## Related types

- [SessionCloseReason](/typescript-sdk-reference/types/sessionclosereason)
