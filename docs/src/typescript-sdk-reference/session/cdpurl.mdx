---
title: "cdpUrl"
---
import AgentMdNotice from '/partials/agent-md-notice.mdx';

<AgentMdNotice />

{/* Generated by node-sdk/scripts/generate-reference.mjs. Do not edit. */}

[Source: node-sdk/src/session.ts](https://github.com/nottelabs/notte/blob/main/node-sdk/src/session.ts#L558)

Get the Chrome DevTools Protocol WebSocket URL for the session.

A `cdp_url` given at construction (another browser provider) is returned
untouched. Notte-served URLs, from the start response or the debug info,
carry the database preview branch when one is configured.

```typescript
Session.cdpUrl(): Promise<string>;
```

## Returns

```typescript
Promise<string>
```

The CDP WebSocket URL for connecting a browser automation client.

## Raises

- Error if no session ID or CDP URL is available.

## Example

```typescript
const browser = await chromium.connectOverCDP(await session.cdpUrl());
```
