---
title: "upload"
---
import AgentMdNotice from '/partials/agent-md-notice.mdx';

<AgentMdNotice />

{/* Generated by node-sdk/scripts/generate-reference.mjs. Do not edit. */}

[Source: node-sdk/src/files.ts](https://github.com/nottelabs/notte/blob/main/node-sdk/src/files.ts#L108)

Upload bytes as a session file. `filename` is what the browser sees when
the file is later attached to a page.

```typescript
SessionFiles.upload(file: Blob | Uint8Array, filename?: string): Promise<SessionFile>;
```

## Parameters

<ParamField body="file" type={"Blob | Uint8Array"} required>
Bytes to upload as a Blob, File, or buffer.
</ParamField>

<ParamField body="filename" type={"string"}>
Stored filename, defaulting to `file`.

Default:

```typescript
'file'
```
</ParamField>

## Returns

```typescript
Promise<SessionFile>
```

Metadata for the uploaded file, including its ID, name, and size.

## Raises

- NotteAPIError if the storage API request fails.

## Related types

- [SessionFile](/typescript-sdk-reference/types/sessionfile)
