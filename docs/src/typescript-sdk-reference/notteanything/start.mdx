---
title: "start"
---
import AgentMdNotice from '/partials/agent-md-notice.mdx';

<AgentMdNotice />

{/* Generated by node-sdk/scripts/generate-reference.mjs. Do not edit. */}

[Source: node-sdk/src/anything.ts](https://github.com/nottelabs/notte/blob/main/node-sdk/src/anything.ts#L48)

Start building a function for `task`. Building is slow (minutes) and
billable; prefer the marketplace (`client.functions.list()`) when a
ready-made function already fits.

```ts
const result = await client.anything.start({ task: 'get the weather in Paris' });
if (typeof result.function_id === 'string') {
  const fn = client.NotteFunction({ function_id: result.function_id });
}
```

Rejects with a `NotteAPIError` when the API answers with a non-2xx status.

```typescript
NotteAnything.start(body: AnythingStartOptions): Promise<AnythingStartResponse>;
```

## Parameters

<ParamField body="body" type={"AnythingStartOptions"} required>

</ParamField>

## Returns

```typescript
Promise<AnythingStartResponse>
```

## Related types

- [AnythingStartOptions](/typescript-sdk-reference/types/anythingstartoptions)
- [AnythingStartResponse](/typescript-sdk-reference/types/anythingstartresponse)
