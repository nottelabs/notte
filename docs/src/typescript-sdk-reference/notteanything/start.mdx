---
title: "start"
---
import AgentMdNotice from '/partials/agent-md-notice.mdx';

<AgentMdNotice />

{/* Generated by node-sdk/scripts/generate-reference.mjs. Do not edit. */}

[Source: node-sdk/src/anything.ts](https://github.com/nottelabs/notte/blob/main/node-sdk/src/anything.ts)

Start a run for `task` and stream the agent's progress back.

Running is slow (minutes) and billable; prefer the marketplace
(`client.functions.list()`) when a ready-made function already fits.

The returned `chunks` generator holds the connection open — iterate it to
completion, or `return`/`break` out of the loop to cancel and release the
stream.

```ts
const run = await client.anything.start({ task: 'get the weather in Paris' });
for await (const chunk of run.chunks) {
  if (chunk.type === 'text-delta') process.stdout.write(String(chunk.delta ?? ''));
}
```

Rejects with a `NotteAPIError` when the API answers with a non-2xx status.

```typescript
NotteAnything.start(body: AnythingStartOptions): Promise<AnythingStartResult>;
```

## Parameters

<ParamField body="body" type={"AnythingStartOptions"} required>

</ParamField>

## Returns

```typescript
Promise<AnythingStartResult>
```

## Related types

- [AnythingStartOptions](/typescript-sdk-reference/types/anythingstartoptions)
- [AnythingStartResult](/typescript-sdk-reference/types/anythingstartresult)
