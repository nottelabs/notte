```python fork_function.py
from notte_sdk import NotteClient

client = NotteClient()

function = notte.Function("<any-public-function-id>")
# creates a private copy of the function (only works for public functions)
forked = function.fork()
```
