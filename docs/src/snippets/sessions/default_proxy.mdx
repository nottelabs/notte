{/* Auto-generated mdx file. Do not edit! */}
{/* @sniptest testers/sessions/default_proxy.py */}

<CodeGroup>

```python default_proxy.py
from notte_sdk import NotteClient

client = NotteClient()

# Start a session with built-in proxies
with client.Session(proxies=True) as session:
    _ = session.execute(type="goto", url="https://www.notte.cc/")
    _ = session.observe()
```

```typescript default_proxy.ts
import { NotteClient } from 'notte-sdk';

const client = new NotteClient();

const session = client.Session({
  proxies: true,
});
await session.use(async () => {
  await session.execute({ type: 'goto', url: 'https://www.notte.cc/' });
  await session.observe();
});
```

</CodeGroup>
