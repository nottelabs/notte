{/* Auto-generated mdx file. Do not edit! */}
{/* @sniptest testers/sessions/captcha/ensure_requirements.py */}

<CodeGroup>

```python ensure_requirements.py
from notte_sdk import NotteClient

client = NotteClient()

# Ensure all requirements are met
with client.Session(
    solve_captchas=True,  # Must be enabled
    proxies=True,  # Helps with detection
) as session:
    pass
```

```typescript ensure_requirements.ts
import { NotteClient } from 'notte-sdk';

const client = new NotteClient();

const session = client.Session({
  solve_captchas: true, // Must be enabled
  proxies: true, // Helps with detection
});
await session.use(async () => {
  // Use your session here
});
```

</CodeGroup>
