<CodeGroup>
  ```python Python
  # Deploy as shared
  function = client.Function(
      workflow_path="my_function.py",
      name="Public Scraper",
      shared=True  # Make publicly accessible
  )
  ```
</CodeGroup>
