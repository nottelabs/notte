<CodeGroup>
  ```python Python
  # Run latest version (default)
  result = function.run(
      url="https://example.com"
  )

  # Run specific version
  result = function.run(
      url="https://example.com",
      version="v2"
  )
  ```
</CodeGroup>
