<CodeGroup>
  ```python Python
  def run(url: str):
      client = NotteClient()

      # Set session timeout
      with client.Session(timeout_minutes=5) as session:
          session.execute(type="goto", url=url)
          data = session.scrape()

      return data
  ```
</CodeGroup>
