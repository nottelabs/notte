<CodeGroup>
  ```python Python
  from notte_sdk import NotteClient

  client = NotteClient()

  with client.Session(
      auth_ids=["<connection-id>"],
      wait_for_authentication=True,
  ) as session:
      session.execute(type="goto", url="https://example.com")
  ```
</CodeGroup>
