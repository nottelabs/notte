{/* Auto-generated mdx file. Do not edit! */}
{/* @sniptest testers/scraping/content_filtering.py, testers/scraping/content_filtering.ts */}

<CodeGroup>

```python Python
# Only main content (excludes navbars, footers, sidebars)
markdown = client.scrape(url, only_main_content=True)  # Default

# Include all page content
markdown = client.scrape(url, only_main_content=False)
```

```typescript TypeScript
// Only main content (excludes navbars, footers, sidebars)
let markdown = await client.scrape(url, { only_main_content: true }); // Default

// Include all page content
markdown = await client.scrape(url, { only_main_content: false });
```

</CodeGroup>
