{/* Auto-generated mdx file. Do not edit! */}
{/* @sniptest testers/agents/structured-output/bp_specific_task.py */}

```python bp_specific_task.py
with client.Session() as session:
    agent = client.Agent(session=session)

    # Good - task matches response_format
    result = agent.run(
        task="Extract the product name, price, and stock status",
        response_format=Product,
    )

    # Less clear - agent might not fill all fields
```
